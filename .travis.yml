sudo: false
language: python
cache: pip
python:
  - "3.6"
env:
  - BASEDIR="file://`pwd`"
install:
  - curl -s -S $BASEDIR/tests/install.sh | bash -
script:
  - curl -s -S $BASEDIR/tests/script.sh | bash -
